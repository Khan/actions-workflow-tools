# AUTOGENERATED by action-preprocessor.js from actions-workflow-tools/.github/workflow-templates/pr-actions.yml

name: Eslint!
'on':
  - pull_request
jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        name: '▶️ Setup checkout: get the repo'
      - run: yarn
        name: '▶️ Setup yarn: install dependencies'
      - uses: Khan/flow-coverage-action@master
        with:
          flow-bin: ./node_modules/.bin/flow
        env:
          GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}'
      - uses: Khan/jest-action@master
        with:
          jest-bin: ./node_modules/.bin/jest
        env:
          GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}'
      - uses: Khan/eslint-action@master
        with:
          eslint-lib: ./node_modules/eslint
        env:
          GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}'
      - run: yarn flow
